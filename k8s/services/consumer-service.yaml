apiVersion: v1
kind: Service
metadata:
  name: consumer-service
  namespace: dev
  labels:
    app: consumer-service
spec:
  type: ClusterIP
  selector:
    app: consumer-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000