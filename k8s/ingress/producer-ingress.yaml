apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: producer-service-ingress
  namespace: dev
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: producer.dev.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: producer-service
                port:
                  number: 80
