apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: consumer-allow
  namespace: dev
spec:
  podSelector:
    matchLabels:
      app: consumer-service
  policyTypes:
    - Egress
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: kafka
      ports:
        - protocol: TCP
          port: 9092
